# PDF_OCR_FEATURE_NOTES.md

## 1. 概要

**目的**：PDF読み取り→CSV変換→`inventory_stock`との照合・抽出という新規オプション機能（PDF OCR機能）について、環境構築手順・実装内容・決定事項・動作確認結果をまとめ、次にこの機能に触れる人（別の開発環境でのセットアップ担当者を含む）が同じ調査・試行錯誤をやり直さずに済むようにする。

**対象読者**：`inventory_app`（部品在庫管理アプリ）のコードに触れる開発者。特に、この機能を新しい開発環境でセットアップする人。

**作成日**：2026-09-13

**このファイルを独立させた理由**：既存3ファイル（`BOM_MIGRATION_NOTES.md`・`UI_WORKFLOW_FIXES_NOTES.md`・`PRODUCTION_NG_ENHANCEMENTS_NOTES.md`）は、いずれも「アプリ本体の既存機能に対する調査・修正」を主題としている。本機能は既存の生産・在庫フローに必須の修正ではなく、独立して追加・削除可能な新規オプション機能であり、かつ**OS側の外部ツール（Tesseract・Poppler）のインストールという、他のどの記録にも無い種類のセットアップ手順**を持つため、既存ファイルへの追記では性質が異なりすぎると判断し、新規ファイルとして独立させた。設計決定の索引という性質上、`CANONICAL_DESIGN_DECISIONS.md`には本機能に関するD-番号の追加は行っていない（本機能のみに閉じた実装判断であり、他ファイルを横断する「確定した設計決定」の索引に載せるものではないと判断したため）。

---

## 2. 背景・機能構成

月次データの在庫値入力に関連するPDF（96コード・名称・数量等が記載され、複数部署の情報が混在する）を取り込み、当部署の部品のみを抽出したいという要望から新規追加した。

機能は2段階に分離している：

1. **PDF→CSV変換**：テキストPDF・画像（スキャン）PDFの両方に対応する汎用読み取り機能。
2. **CSV照合・抽出**：変換結果を`inventory_stock`（在庫値入力に既に取り込まれている96コード一覧。これを「当部署の部品マスタ」とみなす）と照合し、一致する行のみを抽出する機能。

---

## 3. 環境構築手順（次の開発環境でのセットアップ用）

新しい開発環境でこの機能を動かす場合、以下を**順番に**実施すること。

### 3.1 Pythonパッケージのインストール

```
pip install pdfplumber pytesseract pdf2image
```

`inventory_app/requirements.txt`にも追記済みのため、通常の`pip install -r requirements.txt`でも導入される。PyPI経由のため、この手順にはインターネットアクセスが必要（社内ネットワークで到達可能であることを確認済み）。

### 3.2 Tesseract OCR本体のインストール

Pythonの`pytesseract`はTesseract本体を同梱しないラッパーのため、OS側に別途インストールが必要。

```
winget install --id UB-Mannheim.TesseractOCR -e --silent --accept-package-agreements --accept-source-agreements
```

デフォルトでは`C:\Program Files\Tesseract-OCR\tesseract.exe`にインストールされる（`config.py`の`TESSERACT_CMD`のデフォルト値と一致させてあるため、標準的なインストールであれば`config.py`の変更は不要）。

**重要な注意点**：この標準インストールには**日本語データ（`jpn.traineddata`）が含まれていない**（英語＋OSD検出用のみ）。また、`Program Files`配下は多くの開発環境で書き込み権限が無く、後から言語データを追加できない。そのため、以下の手順で日本語データをプロジェクト直下の別フォルダに配置する。

```powershell
# 1. プロジェクト直下に書き込み可能な専用フォルダを作成
mkdir inventory_app\tessdata_local

# 2. 日本語データを取得して配置（tessdata_fast版、精度と速度のバランスを考慮）
Invoke-WebRequest -Uri "https://github.com/tesseract-ocr/tessdata_fast/raw/main/jpn.traineddata" `
  -OutFile "inventory_app\tessdata_local\jpn.traineddata"

# 3. 標準インストール先（Program Files配下、読み取りは可能）から
#    英語・OSDデータもコピーしてくる（jpn単独では初期化に失敗するため両方必要）
copy "C:\Program Files\Tesseract-OCR\tessdata\eng.traineddata" inventory_app\tessdata_local\
copy "C:\Program Files\Tesseract-OCR\tessdata\osd.traineddata" inventory_app\tessdata_local\
```

`inventory_app/tessdata_local/`は`.gitignore`で除外済み（言語データは容量が大きく、かつ環境ごとに個別入手すべきものであるため、Gitでは管理しない）。**つまりこのフォルダは新しい開発環境ごとに毎回この手順を実施して作る必要がある。**

### 3.3 Poppler（pdf2imageの内部依存）のインストール

`pdf2image`はPythonパッケージ単体では完結せず、内部で**Poppler**（`pdftoppm`等の外部コマンド）を呼び出す。Popplerが無いと画像PDF（OCR経路）の変換時にエラーになる（テキストPDF側のpdfplumber経路には影響しない）。

```
winget install --id oschwartz10612.Poppler -e --silent --accept-package-agreements --accept-source-agreements
```

winget版はインストール時にPATHへ自動追加されるが、**既に開いているシェル・既に起動中のプロセスには反映されない**（新しいシェル・PC再起動後・エクスプローラー経由での新規プロセス起動であれば反映される）。

即座に反映させたい場合、または環境によってPopplerのインストール場所が変わり得ることを考慮し、環境変数`POPPLER_PATH`を明示的に設定することを推奨する（`config.py`の`POPPLER_PATH`がこれを参照する）。

```
setx POPPLER_PATH "<pdftoppm.exe等が入っているbinフォルダの絶対パス>"
```

winget経由でインストールした場合の実際のパスは、以下のように検索して特定できる（バージョン番号がパスに含まれるため、バージョンアップ時は変わり得る）。

```powershell
Get-ChildItem -Path "$env:LOCALAPPDATA\Microsoft\WinGet\Packages" -Recurse -Filter "pdftoppm.exe"
```

### 3.4 動作確認

上記3手順が完了していれば、`services/pdf_ocr_service.py`のテキストPDF経路・OCR経路の両方が動作するはずである。テキストPDFのみ試す場合はTesseract・Popplerは不要（`pdfplumber`のみで完結する）。

### 3.5 セキュリティ上の確認事項（重要）

Tesseract OCR本体はインストール後、**完全にローカルで動作しインターネット通信を一切行わない**（PDFの内容が外部に送信されることはない）。インターネット接続が必要なのは、上記3.1〜3.3の**インストール・ダウンロード時のみ**である。社内の機密情報を含むPDFを扱う可能性がある機能のため、この点は導入検討時に確認済みの事項として明記しておく。

---

## 4. 実装内容

### Step1：PDF→CSV変換

- `services/pdf_ocr_service.py`（新規）
  - `extract_pdf_rows(pdf_path)`：テキスト抽出→空ならOCRフォールバック→行分割、という処理フローを持つ。CSVへの書き出しは行わない（プレビュー用）。
  - テキスト/OCR判定：`pdfplumber`で抽出した全ページ合計の文字数が20文字未満（`_MIN_TEXT_LENGTH_THRESHOLD`）であれば「テキスト層が実質無い」とみなし、`pdf2image`＋`pytesseract`によるOCR抽出にフォールバックする。
  - OCR言語は`"jpn+eng"`固定（96コード等の数字・英字と、部品名称等の日本語文字列の混在に対応するため）。
  - 列分解（96コード・名称・数量等への分割）は、現時点で「空白・タブの連続で分割する」素朴な実装（`_split_line_to_columns()`）。**実際のPDFレイアウトが未確定のための暫定実装であり、実物のPDFが判明した時点で調整が必要になる可能性が高い**（§6参照）。
  - `write_rows_to_csv(rows, output_csv_path)`：行データをutf-8-sigのCSVとして保存する（既存の他画面のCSV出力と同じエンコーディング方針）。
  - `convert_pdf_to_csv(pdf_path, output_csv_path)`：上記2つをまとめる便利関数。
- `ui/pdf_ocr_import_window.py`（新規）：PDF選択→変換実行（非同期、`LoadingWindow`＋`threading.Thread`＋`queue.Queue`＋ポーリング、既存の非同期パターンを踏襲）→結果プレビュー→CSV保存、という流れ。

### Step2：照合機能

- `models/inventory.py::list_inventory_part_nos()`（新規）：`inventory_stock`に登録済みの96コード一覧を集合で返す（`part_no`はPRIMARY KEYのため元々重複無し）。
- `services/pdf_ocr_service.py::match_against_inventory(csv_rows)`（新規）：
  - `_PART_NO_PATTERN = re.compile(r"^96\d{5,6}$")`（96コードは"96"で始まる7〜8桁の数字。既存のBOM関連コードと同じ想定パターン）で行内の各セルを厳密一致判定し、`_find_part_no_in_row()`で最初に一致したセルをその行の96コードとみなす。
  - `list_inventory_part_nos()`と突き合わせ、一致する行のみを`matched_rows`として返す。一致しない行（96コードが見つからない行・在庫登録が無い他部署の部品と想定される行）は合算して`excluded_count`とする（内訳は区別しない）。

**既知の限界**：96コードパターンによる列特定は、たとえば数量列がたまたま「9600000」のような7〜8桁の"96"始まり数値になった場合に誤認するリスクを構造的に持つ（同一行内で複数セルが一致した場合は先頭のセルを優先する仕様のため、通常は96コード列の方が先に来ることを前提にしている）。実運用PDFのレイアウトが判明した際、列位置や区切り文字による、より厳密な判定への差し替えが必要になる可能性が高い（§6参照）。

### Step3：左右ペイン化

当初は「照合・抽出」ボタンで単一のプレビューが上書きされる作りだったが、変換結果（全行）と照合抽出結果（一致行のみ）を同時に見比べたいという要望から、既存のNG入力画面（`ui/ng_input_window.py`）・仕掛展開画面（`ui/wip_expansion_window.py`）と同じ左右ペイン構成に変更した。

- 左ペイン（`self.tree_all`）：変換結果（全行）。
- 右ペイン（`self.tree_matched`）：照合・抽出結果（一致行のみ）。照合実行後も**左ペインは一切変更されない**（元データがそのまま残る）。
- 左右それぞれ独立した「CSV保存」ボタン（`on_save_all_csv()`／`on_save_matched_csv()`）。

**実装中に発見・対応した副次的な問題**：PDFを再選択・再変換すると、右ペインの照合結果が前回PDFのものである可能性があるため、再選択・再変換のタイミングで右ペインの照合結果を明示的にリセットする処理を追加した。指示にはなかったが、古い照合結果が新しい変換結果と紐付かないまま残ってしまう不整合を防ぐための必要な対応と判断した。

---

## 5. メインメニューへの組み込み

「8. PDF読み取り（在庫照合）」として、月次データセクションの末尾に追加した（既存の1〜7の番号は崩さず末尾追加）。

**判断理由**：`inventory_stock`を扱う点では「5. 在庫値入力」「7. 在庫値出力」と関連が深いものの、月をまたいで使い回すマスタ的な性質ではなく、都度PDFを取り込む運用データの入力経路のため、共通マスタ側ではなく月次データ側が適切と判断した。既存項目1〜7は番号が既にドキュメント・利用者に定着しているため、割り込ませて振り直すのではなく末尾に追加する形にした。

---

## 6. 動作確認の要点

- テキストPDF・画像（擬似スキャン）PDF（`reportlab`で作成したテキストPDFをもとに、`Pillow`で同内容を画像化しテキスト層無しのPDFへ埋め込んだもの）の両方で、96コード・数量・日本語名称が正しく抽出されることを確認した。ただし合成テストデータによる確認のため、**実際のスキャン品質では精度が下がる可能性がある**点は留保事項として明記する。
- 96コードパターン判定の境界値検証（4桁・6桁・9桁は非一致、7桁・8桁は一致）、紛らわしいパターン（"ABC9600001"等、"96"で始まらないもの）の非一致確認、名称列に数字が埋め込まれたケース（例："部品960001個"）の誤認防止を確認した。
- `inventory_stock`に登録済みの96コードを含むテストデータで正しく一致行が抽出されること、登録の無い96コード（他部署想定）が正しく除外されること、一致件数・除外件数のメッセージが正しく表示されることを確認した。
- 左右ペイン化後、照合実行前後で左ペインの内容が完全に同一であること（右ペインのみ更新される）、CSV保存が左右それぞれ独立して正しい内容（全行／一致行のみ）を保存することを確認した。
- 変換処理中（特にOCR経路）、`LoadingWindow`が表示され、`root.update()`の各呼び出しが短時間で返り続けUIスレッドがブロックされていないことを確認した。
- 各実装ステップ後、`python -m pytest tests/`への影響が無いことを確認済み（ドキュメント作成時点で`1 passed`）。

---

## 7. 粗悪PDF対策（Step1〜3、新機能拡張）

### 背景

§4のPDF OCR機能は「非常にクリーンな合成画像」でのみ検証済みで、実際のスキャン品質（低解像度・傾き・汚れ・低コントラスト等）への耐性が未検証だった。「粗悪なPDFだった場合の対処は」という問いから、以下の3段階の対策を全て実装する方針で着手した。

### Step1：OCR前の画像前処理

新規ライブラリ`opencv-python-headless`を導入した（本アプリはTkinter製デスクトップアプリでOpenCVのGUI機能を使わないため、依存の軽いheadless版を選定。`requirements.txt`に`opencv-python-headless>=4.9.0`として追加）。

`services/pdf_ocr_service.py::preprocess_image_for_ocr()`の処理順序：グレースケール化→傾き補正→ノイズ除去→コントラスト強調→二値化。採用手法と選定理由：

| 処理 | 採用手法 | 選定理由 |
|---|---|---|
| 傾き補正 | `cv2.minAreaRect`による角度推定＋`warpAffine`回転（`_deskew()`） | ハフ変換は罫線が明確でない一般文書では不安定。テキスト塊全体の外接矩形の方が単純で頑健。角度0.5度未満は補正スキップ（クリーン画像の不必要な劣化を防止） |
| ノイズ除去 | `cv2.fastNlMeansDenoising`（Non-Local Means） | **当初は軽いガウシアンブラーを採用したが、実験で「粒状ノイズを十分に均せないまま二値化すると、二値化後に無数の斑点が発生しかえって認識率が落ちる」ことが判明**。計算コストは上がるが文字エッジを保ちながらノイズ除去できるNon-Local Meansに変更した |
| コントラスト強調 | CLAHE（`cv2.createCLAHE`） | 単純なヒストグラム均等化は画像全体を一様に引き伸ばすため、スキャンにありがちな局所的な明暗差（用紙の一部だけ影で暗い等）には効果が薄い。タイル単位で局所的に均等化するCLAHEを採用 |
| 二値化 | 適応的二値化（`cv2.adaptiveThreshold`、ガウシアン重み付き） | 照明ムラのある画像でも安定した黒文字・白背景の二値画像を得やすい |

**重要な発見（失敗からの学び）**：初期パラメータ（軽いガウシアンブラー＋ブロックサイズ31／定数15）では、前処理を入れた方がOCR結果が悪化する現象を発見した。パラメータのスイープ調査で「素のOCRでは一部失敗する境界付近」を意図的に狙った劣化画像テストで、前処理なし＝2/3認識、前処理あり（修正後）＝3/3認識、という改善を確認した。

処理時間：`preprocess_image_for_ocr()`単体の平均処理時間は約199ms/回（900×400px画像）。OCR自体（数百ms〜）と比べて体感できる増加ではない。

### Step2：OCR信頼度スコアの可視化

- `pytesseract.image_to_string()`（単純なテキスト取得）から`pytesseract.image_to_data()`（信頼度スコア付き詳細データ）に変更（`_extract_lines_via_ocr()`・`_group_words_into_lines()`）。
- `extract_pdf_rows()`の戻り値に`confidences`（行ごとの平均信頼度、0〜100）を追加。テキスト抽出経路（OCR不使用）は一律`100.0`。
- 左ペインに「信頼度」列を追加し、閾値（`ui/pdf_ocr_import_window.py::_LOW_CONFIDENCE_THRESHOLD = 70`）未満の行を赤系の背景色（`#ffb3b3`）で強調表示。

**重要な発見**：`image_to_data()`は日本語（CJK）の各文字を個別の"word"として返すことが多く、単純に空白連結すると行が文字単位まで過剰分割される現象を発見した。単語間の画素ギャップから空白挿入を判定する対策も試したが、文字ごとの検出高さ・幅が不安定でかえって誤判定を招くため見送り、既知の制限として`_group_words_into_lines()`のdocstringに明記した。**ただし96コード等の数字列は1つの連続した単語として認識されるため、信頼度算出・96コード列判定のいずれにも実害はないことを確認済み**（既存の照合機能に影響しない）。

### Step3：プレビューの手動修正機能

- 左ペイン（変換結果全行プレビュー）にセル編集機能を追加。既存の`ui/checkable_treeview.py::CheckableTreeview`の編集パターン（ダブルクリック→セル上にEntryを重ねる→Return/FocusOutで書き戻し→Escapeでキャンセル）を踏襲しつつ、**`CheckableTreeview`をそのまま流用せず新規実装**した（`CheckableTreeview`の検証ロジックは`float()`変換による数値専用バリデーションで、96コード・名称・数量が混在するこの画面のテキストには適さないと判断したため）。
- 「信頼度」列は編集対象から除外。
- 変換完了時、信頼度が閾値未満の行が1件でもあれば、該当行を自動選択・スクロール表示した上で「信頼度が低い行がN件あります。内容を確認し、誤認識があればセルをダブルクリックして修正してください。」と案内する（`_notify_low_confidence_rows_if_any()`）。

### 検証（粗悪PDF対策の核心シナリオ）

誤認識を想定した96コード（`96000099`、在庫に存在しない）を含む変換結果に対し、「照合・抽出」すると当然除外されるが、**セル編集で正しい値（`96000002`）に手動修正した後、再度「照合・抽出」を実行すると、修正後の内容で正しく一致する**ことを実際に確認した。これにより「OCRが誤認識しても、人間が気づいて手動修正できる」という当初の目的が実証された。

### 検証手法上の注記

自動テストでは、ヘッドレス（`withdraw()`済み）ウィンドウで実際の`<Return>`キーイベント（`event_generate`）が確実に配送されないため、`_commit_cell_edit()`を直接呼び出す形で確認した。同様に信頼度列のクリック判定は、`identify_region`／`identify_row`／`identify_column`を一時的に差し替えて列判定ロジック自体を検証した。**いずれも検証手法上の制約であり、実装側の問題ではない**。

---

## 8. 未解決・今後の課題

- **実際のPDFサンプルでの精度検証は依然未実施**（手元に実物が無いため、§7の画像前処理・信頼度可視化・手動修正も含め、これまでの検証は全て合成テストデータに留まる）。実際にスキャンされたPDFが入手でき次第、列分解ロジック（`_split_line_to_columns()`）・96コード判定パターン（`_PART_NO_PATTERN`）に加え、画像前処理のパラメータ（傾き検出閾値・ノイズ除去強度・二値化のブロックサイズ等）・信頼度の警告閾値（`_LOW_CONFIDENCE_THRESHOLD = 70`）の調整が必要になる可能性が高い。
- **Tesseract本体の配布方法**（.exe化した各PCへの配布時、Tesseract本体を同梱するか、各PCに個別インストールしてもらうか）は、この機能単体の課題ではなく、アプリ全体の.exe化（`UI_WORKFLOW_FIXES_NOTES.md` §4「.exe化」）に取り組む段階で改めて決定する方針のまま変わらない。**重複記載を避けるためここでは詳細を持たない**（決定した際は`UI_WORKFLOW_FIXES_NOTES.md`側を更新し、本ファイルからは参照のみとする）。

---

## 9. 関連ファイルへのポインタ

- 共有フォルダ運用・ロック機構・堅牢性調査など、本機能とは別系統の未完了タスクは`UI_WORKFLOW_FIXES_NOTES.md` §4「未対応・将来の検討事項」を参照。
- NG一覧・仕掛一覧関連の未完了タスク（「対象外」マーク機能等）は`PRODUCTION_NG_ENHANCEMENTS_NOTES.md` §6を参照。
- 本機能が参照する既存の左右ペインUIパターンの詳細は`PRODUCTION_NG_ENHANCEMENTS_NOTES.md` §8（仕掛展開画面）・`ui/ng_input_window.py`を参照。
